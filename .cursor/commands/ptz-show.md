# Ptz Show

Show the user's current status of tasks and priorities.

Run this command and return the output exactly:
`npx tsx ptz.ts show`

Show the user the literal output from the `show` command, including any formatted tables and other data structures.
